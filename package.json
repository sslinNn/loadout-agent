{
  "name": "loadout-agent-workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=@loadout/shared && npm run build --workspace=loadout-agent",
    "test": "npm test --workspace=@loadout/shared && npm test --workspace=loadout-agent"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vitest": "^2.1.0",
    "@types/node": "^20"
  }
}
